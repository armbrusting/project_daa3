<h1>Sign up</h1>

<%= form_for(@user, :html => { :multipart => true }) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="field">
	<%= f.file_field :avatar %>
  </div>
  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>