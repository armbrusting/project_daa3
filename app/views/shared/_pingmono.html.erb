<%= form_for Notice.create(:folder_id => @folder.id, :attention => "monopol"),  :remote => true do |f| %>
  <div><%= f.hidden_field :folder_id %></div>
  <div><%= f.hidden_field :attention %></div>
  <div class="actions"><%= f.submit "Ping Monopol" %></div>
<% end %>	